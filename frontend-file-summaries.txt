{
  "frontend/src/App.tsx": "Main app component with updated default imports for Header, Hero, Services, EnrollmentCTA; handles all view navigation including aliases like 'courses', 'sessions', 'enquiry', 'dashboard'; adds pt-16/pt-20 padding for fixed header.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations including new `useAddOrUpdateMeetingLink` and `useRemoveMeetingLink` mutation hooks that call the backend meeting link endpoints and invalidate session queries on success.",
  "frontend/src/components/Hero.tsx": "Full-screen hero section with math-themed animated background, premium navy/gold design; uses `StudentEnquiryForm` with `onBack` prop (not `onClose`) and default export.",
  "frontend/src/components/ProblemGenerator.tsx": "Interactive problem generator with authentication-gated access, topic and difficulty selection (Foundational/Intermediate/Advanced), backend-integrated answer validation, and step-by-step solution display for general math enthusiasts.",
  "frontend/src/components/ProgressTracker.tsx": "Progress tracking dashboard displaying authenticated user's mathematical metrics including total problems solved, difficulty breakdown (Foundational/Intermediate/Advanced), topic-wise performance with accuracy percentages, course purchase status badge, and recent solutions.",
  "frontend/src/components/Footer.tsx": "Application footer exported as a named export `Footer`, with brand info, quick links including Student Portal with graduation cap icon, contact info, copyright, and caffeine.ai attribution with UTM tracking.",
  "frontend/src/index.css": "Global styles with OKLCH color system adding status color CSS variables (--status-pending, --status-confirmed, --status-rejected) alongside the existing navy/gold/cream palette.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with status-pending, status-confirmed, and status-rejected color tokens mapped to CSS variables for use in enquiry status badges.",
  "frontend/src/components/LoginButton.tsx": "Standalone authentication button component; logout state now uses high-contrast red (`bg-red-600 text-white`) styling for clear visibility, while login state uses the gold theme color with navy text, both with border accents for added definition.",
  "frontend/src/components/ProfileSetup.tsx": "User profile setup modal that prompts first-time authenticated users to enter their name before accessing the application, with form validation, backend integration, and initialization of hasPurchasedCourse field to false.",
  "frontend/src/components/CourseOffering.tsx": "Course purchase component using restored `useGetCourseDetails` and `useRecordUPIPayment` hooks from `useQueries`, with inline UPI payment dialog.",
  "frontend/public/manifest.json": "PWA manifest file rebranded to \"Rajat's Equation - Mathematics Platform\" with updated description emphasizing general mathematical learning for all enthusiasts.",
  "frontend/public/service-worker.js": "Service worker implementing install, activate, and fetch events with cache-first strategy for static assets and network-first for API calls to enable offline functionality.",
  "frontend/index.html": "HTML entry point with updated title \"Rajat's Equation - Master Mathematical Thinking\", PWA meta tags with navy blue theme color, and manifest link.",
  "frontend/src/main.tsx": "Application entry point with React root rendering, providers setup, and service worker registration for PWA functionality.",
  "frontend/src/hooks/useInstallPrompt.ts": "Custom React hook managing PWA install prompt state by capturing beforeinstallprompt event and providing installApp function to trigger installation.",
  "frontend/src/components/Services.tsx": "Course cards grid with premium navy/gold design; fixes invalid `ringColor` CSS property by removing it, and uses `StudentEnquiryForm` with `onBack` prop instead of `onClose`.",
  "frontend/src/components/EnrollmentCTA.tsx": "Enrollment CTA section using named import `{ LoginModal }` from LoginModal; features navy gradient card with gold CTA buttons and benefit cards.",
  "frontend/src/components/LoginModal.tsx": "New reusable login modal dialog that prompts unauthenticated users to sign in via Internet Identity, with a loading spinner during login and a dismiss option to continue browsing.",
  "frontend/src/components/AuthGate.tsx": "New reusable authentication gate component that wraps interactive elements — if the user is unauthenticated it intercepts clicks and shows the LoginModal; if authenticated it executes the provided onAction callback.",
  "frontend/src/config/razorpay.ts": "Razorpay configuration file exporting the placeholder Key ID constant (with clear comments to replace it), business metadata, and TypeScript type declarations for Razorpay options, payment response, and the global window.Razorpay constructor.",
  "frontend/src/hooks/useRazorpay.ts": "Custom React hook that dynamically loads the Razorpay checkout.js script from CDN and exposes an `openRazorpay` function that accepts full Razorpay options and opens the payment modal, with loading state tracking.",
  "frontend/src/types/booking.ts": "TypeScript type definitions for `BookingFormData` (with `date`, `time`, `serviceType`, `classType`, `numberOfClasses`, `discountPercent`, `finalAmount`), `BookingRecord`, and `UpiPaymentParams` — restored to match what `BookingForm` and `BookingFlowManager` expect.",
  "frontend/src/hooks/useBooking.ts": "React Query hooks for booking record operations — `useAddBookingRecord` mutation to persist a booking to the backend, and `useGetBookingRecords` query to fetch all records (admin-only).",
  "frontend/src/components/BookingForm.tsx": "Booking form component with all fields (name, phone, service, class type, number of classes, date/time) and a prominent gold-background submit button with navy text, arrow icon, and clear hover/disabled states for high visibility.",
  "frontend/src/components/BookingConfirmation.tsx": "Success confirmation screen displaying all booking details including an optional `discountCode` badge with percentage, final amount, and next-steps instructions.",
  "frontend/src/components/BookingFlowManager.tsx": "Booking flow dialog with form, UPI payment, and confirmation steps; exports both named `BookingFlowManager` and default export for backward compatibility with all existing import styles.",
  "frontend/src/utils/upiPayment.ts": "Utility module for UPI payment URL generation with the hardcoded UPI ID (9424135055@ptyes), including a mobile device detection helper.",
  "frontend/src/components/UpiPaymentStep.tsx": "UPI payment step component with client-side discount code validation for SAVE10 (10%), SUMMER20 (20%), RAJAT50 (50%), and MATHS30 (30%) codes — validation is triggered only on button click, is case-insensitive, shows success/error feedback, and correctly applies the percentage discount to the displayed amount, QR code, and payment confirmation.",
  "frontend/src/components/AdminDashboard.tsx": "Fully rebuilt admin dashboard with all hooks imported via proper ES imports (no `require()`), persistent left sidebar with grouped navigation, and an Analytics tab that embeds the BookedStudentsSection subsection for managing booked students.",
  "frontend/src/components/CompletedSessions.tsx": "Public-facing page displaying all completed tutoring sessions as cards, using the correctly named useGetCompletedBookings hook from useQueries.",
  "frontend/src/utils/pricingCalculator.ts": "New utility module that exports `calculateBookingPrice`, `getPricePerClass`, `getDiscountPercent`, per-service price maps, and discount tier constants for the booking pricing logic (20% off 10+, 30% off 15+, 35% off 20+ classes).",
  "frontend/src/components/CourseMaterialsManager.tsx": "Admin component for managing course materials per course, with a course selector dropdown, a list of existing materials with type badges (PDF/video/note/link) and delete buttons, and an add-material form with title, type, and URL fields.",
  "frontend/src/components/ClassSessionsManager.tsx": "Admin component for managing class sessions per course, with a course selector, a chronologically sorted list of sessions showing date/time/Google Meet/Calendar links with delete buttons, and an add-session form with all required fields.",
  "frontend/src/components/StudentPortal.tsx": "Student portal with access code entry and four-tab layout (Materials, Schedule, Support, Attendance); uses `any` type for booking record to avoid missing backend types, passes correct props to `StudentScheduleTab` (with `studentPrincipal`) and `StudentAttendanceView` (with `course`).",
  "frontend/src/components/StudentMaterialsTab.tsx": "Admin/student component for managing course materials per course name; removes dependency on `CourseMaterialType` from backend, uses local string union type, and calls `useDeleteCourseMaterial` with a plain string title argument.",
  "frontend/src/components/StudentScheduleTab.tsx": "Admin component for managing per-student class schedule entries, now with meeting link support — platform selector and URL input in the add-session form, inline edit/delete for existing meeting links, and proper confirmation dialogs.",
  "frontend/src/components/AdminSupportMessages.tsx": "Admin support messages component using `studentPrincipal` (not `studentId`) as the key for `replyMutation.mutateAsync` to match the hook's expected parameter shape.",
  "frontend/src/components/StudentSupportView.tsx": "Student support view using the restored `useGetMySupportMessages` hook (accepting `Principal | null`) and `useSubmitSupportMessage`.",
  "frontend/src/components/AttendanceManager.tsx": "Attendance management table that removes the broken `useGetAttendanceRecords()` call (which required arguments) and instead tracks attendance state locally; marks present/absent via `useMarkAttendance` mutation.",
  "frontend/src/components/StudentAttendanceView.tsx": "Student-facing attendance view showing summary stats and per-session records; accepts optional `studentPrincipal`, `bookingId`, and `course` props (no `courseName`), falling back to the logged-in user's identity.",
  "frontend/src/components/DiscountCodeManager.tsx": "Admin discount code management component that displays a built-in codes table listing all four active codes (SAVE10, SUMMER20, RAJAT50, MATHS30) with their percentages, a code generator form for reference codes, and a separate table for backend-managed codes with deactivate buttons.",
  "frontend/src/components/VisitorTrackingView.tsx": "Admin visitor tracking view using the correctly named `useBookingRecords` hook, displaying booking count stats and an informational message about the missing getAllVisitorActivities backend endpoint.",
  "frontend/src/components/Header.tsx": "Sticky navigation header using named import `{ LoginButton }` from LoginButton; features navy/gold premium design with active state indicators, mobile hamburger menu, and admin tab support.",
  "frontend/src/components/PaymentEnquiryForm.tsx": "Payment enquiry form that collects student name, contact, fee type, amount, and duration; fixes the type error by converting the parsed amount to `bigint` using `BigInt()` before passing to the mutation hook.",
  "frontend/src/components/StudentEnquiryForm.tsx": "New student-facing enquiry submission form with fields for name, contact info, course interest, and message; shows a confirmation screen with the enquiry ID and copy button after successful submission.",
  "frontend/src/components/AdminEnquiriesTab.tsx": "New admin component displaying all student enquiries as expandable cards with status badges (pending/confirmed/rejected), Confirm/Reject action buttons for pending enquiries, and inline QR code display for confirmed enquiries.",
  "frontend/src/components/StudentEnquiryPortal.tsx": "Student-facing portal for checking enquiry status by ID; removes import of non-existent `EnquiryStatus` from backend and uses a local `getStatusKey` helper to handle variant-style status objects.",
  "frontend/src/components/QRCheckInView.tsx": "QR check-in view supporting camera scanning and manual token entry; fixes the module import path from `'qr-code/useQRScanner'` to the correct relative path `'../qr-code/useQRScanner'`.",
  "frontend/src/components/StudentDetailView.tsx": "Admin component for managing a specific student's sessions and materials, now with full meeting link support — platform selector (Google Meet, Zoho Meet, Zoom), URL input, inline edit form pre-filled with existing data, and confirmation dialog for removing meeting links.",
  "frontend/src/components/StudentDashboard.tsx": "Student-facing dashboard showing assigned sessions and materials in a tabbed layout, with a prominent 'Join Class' button (with platform badge) for sessions that have a meeting link, opening the URL in a new tab.",
  "frontend/src/components/BookedStudentsSection.tsx": "Booked students table with search; opens `StudentDetailView` by passing `studentPrincipal`, `studentName`, and `studentPhone` props (matching the component's interface) instead of a `student` object prop."
}